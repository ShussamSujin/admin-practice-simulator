{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "admin-sim-static",
      "runtimeExecutable": "python",
      "runtimeArgs": ["-c", "import os,http.server; http.server.test(HandlerClass=http.server.SimpleHTTPRequestHandler, port=int(os.environ.get('PORT', 8000)))"],
      "autoPort": true
    }
  ]
}
